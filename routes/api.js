const express = require('express');
const router = express.Router();
// const top5Controller = require('../controllers/api/top5/apiController');
// const newContentsController = require('../controllers/api/new-contents/apiController');
// const keywordGapController = require('../controllers/api/keyword-gap/apiController');
// const { verifyToken, checkCredit } = require('../middlewares/auth');

// router.get('/top5', [verifyToken, checkCredit], top5Controller.scrape)
// router.get('/new-contents', [verifyToken, checkCredit], newContentsController.getNewContents)
// router.get('/keyword-gap', [verifyToken, checkCredit], keywordGapController.getKeywordsGap)


module.exports = router